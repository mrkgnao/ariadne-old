-- | 'Query' and 'QueryArr' are the composable units of database
-- querying that are used in Edible.

module Edible.QueryArr (Query, QueryArr) where

import           Edible.Internal.QueryArr (QueryArr, Query)
